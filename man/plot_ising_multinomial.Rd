% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_graph.R
\name{plot_ising_multinomial}
\alias{plot_ising_multinomial}
\title{Plot Ising multinomial distribution from Theta}
\usage{
plot_ising_multinomial(
  theta,
  bias = NULL,
  sort_desc = TRUE,
  max_rows = NULL,
  display = c("table", "barplot"),
  digits = 5,
  main = "Multinomial Distribution from Ising Theta"
)
}
\arguments{
\item{theta}{Coupling matrix (p x p) defining the Ising model.}

\item{bias}{Optional bias vector (length p). If NULL, assumed to be zero.}

\item{sort_desc}{Logical. If TRUE, sort rows by decreasing probability.
Default: TRUE.}

\item{max_rows}{Optional integer. If provided, only the top \code{max_rows}
states are shown in the plot.}

\item{display}{Character. One of \code{"table"} (default) or
\code{"barplot"}.}

\item{digits}{Integer. Number of digits for displayed probabilities.
Default: 5.}

\item{main}{Plot title.}
}
\value{
Invisibly returns a data frame with columns
\code{configuration} and \code{probability}.
}
\description{
Uses \code{generate_ising_to_multinomial()} to compute the multinomial
distribution induced by an Ising coupling matrix, then displays the
probabilities in a table-style plot (or bar plot) with explicit state labels.
}
\examples{
theta <- matrix(0, 3, 3)
theta[1, 2] <- theta[2, 1] <- 0.5
theta[2, 3] <- theta[3, 2] <- -0.4
plot_ising_multinomial(theta, max_rows = 8)

}
