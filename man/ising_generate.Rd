% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generateGraphData.R
\name{ising_generate}
\alias{ising_generate}
\title{Generate binary (±1) samples from an Ising MRF with specified graph structure
and target marginal probabilities.}
\usage{
ising_generate(
  n_nodes,
  edges,
  target_probs = rep(0.5, n_nodes),
  weights = NULL,
  n_samples = 1000,
  burn_in = 500,
  calibrate = TRUE,
  tol = 0.01,
  max_iter = 30,
  verbose = FALSE,
  seed = NULL
)
}
\arguments{
\item{n_nodes}{Integer. Number of nodes.}

\item{edges}{Two-column integer matrix of edges (1-indexed). Each row is
one undirected edge (i, j) with i < j.}

\item{target_probs}{Numeric vector of length \code{n_nodes} with target
P(X_i = +1) for each node. Values must be in (0, 1).
Default: all 0.5.}

\item{weights}{Optional numeric vector of edge weights (same row order as
\code{edges}). If NULL, random weights in \link{-0.8, -0.3} ∪
\link{0.3, 0.8} are used.}

\item{n_samples}{Integer. Number of samples to return.}

\item{burn_in}{Integer. Gibbs burn-in steps.}

\item{calibrate}{Logical. If TRUE (default), bias terms are tuned to match
\code{target_probs}.}

\item{tol}{Convergence tolerance for marginal calibration.}

\item{max_iter}{Maximum calibration iterations.}

\item{verbose}{If TRUE, print calibration progress.}

\item{seed}{Optional random seed for reproducibility.}
}
\value{
A list with:
\describe{
\item{\code{samples}}{Integer matrix (n_samples × n_nodes) of ±1 values.}
\item{\code{theta}}{Coupling matrix used.}
\item{\code{bias}}{Calibrated bias vector.}
\item{\code{target_probs}}{Target marginal probabilities.}
\item{\code{estimated_probs}}{Estimated marginals from calibration run.}
\item{\code{edges}}{Edge matrix.}
\item{\code{colnames}}{Column names for the sample matrix.}
}
}
\description{
Generate binary (±1) samples from an Ising MRF with specified graph structure
and target marginal probabilities.
}
\examples{
edges <- matrix(c(1,2, 2,3, 3,4, 4,5, 1,5), ncol = 2, byrow = TRUE)
probs <- c(0.7, 0.4, 0.6, 0.3, 0.8)
result <- ising_generate(
  n_nodes      = 5,
  edges        = edges,
  target_probs = probs,
  n_samples    = 1000,
  seed         = 42
)
head(result$samples)
result$estimated_probs

}
