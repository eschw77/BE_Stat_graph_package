% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_graph.R
\name{plot_ising_theta}
\alias{plot_ising_theta}
\title{Plot Ising Graph from Theta Matrix}
\usage{
plot_ising_theta(
  theta,
  threshold = 0,
  layout = "circle",
  vertex.size = 25,
  vertex.color = "lightblue",
  edge.color.positive = "steelblue4",
  edge.color.negative = "firebrick3",
  edge.width.range = c(1, 5),
  main = "Ising Graph from Theta",
  show.weights = FALSE
)
}
\arguments{
\item{theta}{Numeric square matrix of Ising couplings.}

\item{threshold}{Numeric. Only edges with \code{|theta[i, j]| > threshold}
are plotted. Default: 0.}

\item{layout}{Character or function specifying the layout algorithm. Options
include "circle", "kamada.kawai", "fruchterman.reingold", and "grid".
Default: "circle".}

\item{vertex.size}{Numeric. Size of the vertices. Default: 25.}

\item{vertex.color}{Character. Color of the vertices. Default: "lightblue".}

\item{edge.color.positive}{Character. Color for positive couplings.
Default: "steelblue4".}

\item{edge.color.negative}{Character. Color for negative couplings.
Default: "firebrick3".}

\item{edge.width.range}{Numeric length-2 vector giving minimum and maximum
edge width used for scaling by \code{|theta|}. Default: \code{c(1, 5)}.}

\item{main}{Character. Main title for the plot.
Default: "Ising Graph from Theta".}

\item{show.weights}{Logical. If TRUE, edge labels show theta values.
Default: FALSE.}
}
\value{
Invisibly returns the igraph object.
}
\description{
Visualizes an Ising graph directly from a coupling matrix \code{theta}.
An undirected edge is drawn between nodes \eqn{i} and \eqn{j} when
\code{|theta[i, j]| > threshold}. Positive and negative couplings are
colored separately, and edge width scales with \code{|theta[i, j]|}.
}
\examples{
theta <- matrix(0, 4, 4)
theta[1, 2] <- theta[2, 1] <- 0.7
theta[2, 3] <- theta[3, 2] <- -0.5
theta[3, 4] <- theta[4, 3] <- 0.3
plot_ising_theta(theta, threshold = 0.1, show.weights = TRUE)

}
